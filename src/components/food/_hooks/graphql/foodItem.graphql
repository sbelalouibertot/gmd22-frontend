query FoodItem($foodItemId: ID!) {
  foodItem(id: $foodItemId) {
    foodItem {
      id
      name
      type
      image
      currentRecipes {
        id
        name
      }
      recipes {
        id
        name
      }
    }
  }
}
