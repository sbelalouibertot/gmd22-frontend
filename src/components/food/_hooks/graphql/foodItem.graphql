query FoodItem($foodItemId: ID!) {
  foodItem(id: $foodItemId) {
    foodItem {
      id
      name
      type
      currentRecipes {
        id
        name
      }
      recipes {
        id
        name
      }
    }
  }
}
