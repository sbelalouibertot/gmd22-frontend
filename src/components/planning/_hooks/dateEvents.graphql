query DateEvents {
  events(onlyCurrentPeriod: true) {
    events {
      id
      type
      date
      recipes {
        id
        name
        preparationDuration
        cookingDuration
      }
      shoppingList {
        id
        name
      }
    }
  }
}