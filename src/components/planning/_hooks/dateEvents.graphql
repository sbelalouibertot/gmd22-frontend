query DateEvents {
  events(onlyCurrentPeriod: true) {
    events {
      id
      type
      date
      recipes {
        id
        name
        preparationDuration
        cookingDuration
        image
      }
      shoppingList {
        id
        name
      }
    }
  }
}