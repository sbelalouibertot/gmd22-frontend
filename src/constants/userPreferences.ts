import { IUserPreferenceType } from '@src/generated/gmd22-api'

export const USER_PREFERENCES_LABELS: Record<IUserPreferenceType, string | string[]> = {
  MAX_RECIPES_PER_WEEK: '🥕 Recettes par semaine ‍',
  SHOPPING_WEEKS_INTERVAL: '🛒 Semaines entre chaque course ',
}
